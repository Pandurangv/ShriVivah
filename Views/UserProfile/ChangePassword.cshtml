@model ShriVivah.Models.RegisterViewModel
@{
    ViewBag.Title = "Change Password";
    Layout = "~/Views/Shared/_LayoutAdminPanel.cshtml";
}
@Scripts.Render("~/bundles/UserProfile")
<h2>Change Password</h2>
@using (Html.BeginForm("", "", FormMethod.Post, new { id = "frmEditUserProfile" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        @Html.HiddenFor(m => m.UserId)
        <input type="hidden" id="ActiveUserId" value="@ViewBag.UserId" />
        @Html.ValidationSummary(true)
        <div class="form-group" id="contentdiv">
            <div id="divPersonal">
                <div class="form-group">
                    <label for="focusedinput" class="col-sm-2 control-label">जुना पासवर्ड</label>
                    <div class="col-sm-8">
                        <input type="password" id="txtOldPassword" class="form-control1" />
                    </div>
                </div>
                <div class="form-group">
                    <label for="focusedinput" class="col-sm-2 control-label">नवीन पासवर्ड</label>
                    <div class="col-sm-8">
                        <input type="password" id="txtNewPassword" class="form-control1"  />
                    </div>
                </div>
            </div>
            <div class="span_3">
                <input type="button" value="सेव करा" class="btn btn-default" id="btnChangePassword"  />
                <input type="button" value="रद्द करा" class="btn btn-default" id="btnCancel" />
            </div>
        </div>
    </div>
}


